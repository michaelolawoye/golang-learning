logs/ :
First Go project, learning the syntax, idiomatic code and project structure. Also learning file I/O. main.go contains a program that count the number of occurrences of a character, given to the -target flag,
in a file, given to the -in flag, the results of which are written in another file, given to -out flag
